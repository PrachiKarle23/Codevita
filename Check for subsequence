class Solution{
    public:
    bool isSubSequence(string A, string B) 
    {
        int j=0;
       for(int i=0;i<B.length() && j<A.length();i++){
           if(A[j]==B[i]){
               j++;
           }
       }
       return j==A.length();
    }
};
